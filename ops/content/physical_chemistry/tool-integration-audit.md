# Инструменты тренажёра: границы и проверка

## Текущее состояние рабочего выпуска

Инструменты включены только для физхимии во всех четырёх ролях. Снимок
`4f5720ed3c1ec7fd29928b94ed43582024ea87ce1bccf2edb6e377f988cec447`
опубликован 2026-09-12. Ниже сохранён хронологический журнал, включая старые
решения «hold»: они относятся к промежуточным проверкам, а не текущему флагу.
Модели и флаги трёх остальных дисциплин сравнены с резервной копией и не изменены.

Рабочие проверки через API тренажёра, по одной задаче (не замер обычного пакета из пяти):

- Михаэлис–Ментен: batch `0ef2b54c27d74b70ad6bf0304783ee5c`, HTTP 201,
  58.08 с. Сохранённый эталон проверен вручную: при K_M=2.50·10⁻⁵ М,
  r_max=4·10⁻⁴ М/с, [E]_0=2·10⁻⁸ М и [S]=10⁻⁵ М получаются
  r=1.142857·10⁻⁴ М/с и k_cat=2·10⁴ с⁻¹.
  Аудит БД: Qwen — два модельных запроса, один успешный calculator-v3;
  DeepSeek — два запроса, три успешных calculator-v3.
  `correction.applied=true`; решение банка точно совпадает с полным
  проверенным решением верификатора, не с устаревшим черновиком генератора.
- Последовательные реакции: batch `6793e2c4263e45c68d178a59b856f958`, HTTP 201,
  107.69 с. Вручную проверены интегрирующий множитель, начальное условие,
  формула [B](t), условие максимума. При [A]_0=0.50 М, k₁=0.040 мин⁻¹,
  k₂=0.080 мин⁻¹: t_max=ln(2)/0.040≈17.33 мин, [B]_max=0.125 М.
- Теория столкновений: batch `e840e463119c4fc2a8b3647c5b705035`, HTTP 503,
  93.66 с, `finish_reason=length`. Кандидат не получен, новая задача не
  сохранена. Это ограничение завершения модели, не предметный отказ
  верификатора. Автоматическая генерация замены не выполнялась.

Проверка студента на опубликованном снимке: задача 1.3 с полным корректным
решением получила 10/10 за 8.15 с. При двух решённых разных задачах кнопка
генерации остаётся закрыта; прямой POST также даёт 403 «решите 3 задачи».
Ошибочная неполная работа 1.1 сначала столкнулась с пустым final после трёх
успешных инструментов. После исправления Rust-адаптера повторная проверка
той же работы завершилась за 27.09 с: ошибка определения порядка по
стехиометрии распознана, будущие численные ответы не раскрыты. В русском
feedback появились посторонние иностранные слова: это дефект качества,
а не успешная проверка стиля; инструкция редакторского самоконтроля усилена
в редактируемом промпте, без дополнительной модели и фильтра.

Полное исправленное решение этой же задачи получило 10/10 за 17.35 с.
Каталог подтвердил 3/3 и `generation_unlock=true` только для формальной
кинетики; для остальных четырёх подтем осталось false. В одном комментарии
по критерию повторился символ µ, хотя общий feedback и оценка были верны.
Поэтому этот прогон подтверждает оценивание и разблокировку, но не достаточное
качество всех текстовых полей. Для следующей проверки параметры Qwen задаются
явно, а не оставляются на усмотрение провайдера: temperature=0.6, top_p=0.95,
presence_penalty=0.0 через карту точных URI. Это исходная настройка для проверки,
не установленная единственная причина и не доказательство устранения дефекта.

Резерв длины ответа задаётся явно через конфигурацию точного URI модели,
не через предметный хардкод. Отсутствие `max_tokens` в запросе не гарантирует
отсутствия ограничения на стороне провайдера. В официальной карточке Qwen
рекомендуется 32 768 для обычных запросов и 81 920 для сложных; здесь выбран
больший резерв, не обязательный расход. Yandex указывает контекст 262 144,
но не документирует на этой странице значение default output limit.
Источники: [Qwen](https://huggingface.co/Qwen/Qwen3.6-35B-A3B/blob/main/README.md),
[Yandex](https://aistudio.yandex.ru/ru/docs/ai-studio/concepts/generation/models).
Подтверждение принятия параметра рабочим API фиксируется отдельно после выпуска.

Дата: 2026-09-12. Пакет: предоставленный `essential_skills` 1.0.0,
адаптированный шлюз v2. Исходный каталог не изменяется.

## Что меняется

Роль модели сохраняется: генератор строит задачу; единственный независимый
верификатор проверяет и исправляет эту же задачу. Калькулятор, SymPy и справочник
— вызываемые моделью инструменты, не дополнительные модели или классификаторы.
Помощник и проверка работы студента получают эталон, но не раскрывают будущие
ответы в режиме пошаговой помощи. Четыре разрешения на инструменты независимы
и по умолчанию выключены; включение физхимии не меняет настройки других курсов.

## Проверенные ограничения

- Числа Decimal читаются из исходной записи, не через float; результат сохраняет
  34 значащие цифры без преждевременного округления.
- SymPy получает объекты из ограниченного AST, не строковый `sympify`/`eval`.
  Недоказанное тождество не означает ошибку решения; область определения
  и химическая применимость остаются предметом анализа модели.
- Расчёт идёт в отдельном процессе с ограничением времени и ресурсов.
  Неверные аргументы возвращаются в тот же диалог для исправления, а не запускают
  генерацию другой задачи. Сетевой сбой не считается химической ошибкой.
- Приватный CSV отсутствует в Git и образе. Подключается read-only; справочник
  возвращает происхождение и SHA256 именно прочитанных данных.
- Исходные 80 записей сохранены без изменения чисел. Это проверка сохранности,
  **не независимая сертификация всех справочных значений**. Ошибочная пометка
  брома как моноизотопного элемента отмечается отдельным предупреждением.
- Секрет доступа известен только серверу. В браузер не передаются ключ,
  внутренние трассы, рассуждения модели и полный справочник.

## Выпуск

Предварительные проверки: 54 теста шлюза, включая сохранность приватной таблицы;
Linux-контейнер non-root/read-only/cap-drop/network-none — четыре параллельных
расчёта и восстановление после ограничения ресурсоёмкого выражения. Лимиты
контрольного контейнера: 2 CPU, 2304 MiB, 32 процесса. Проверки Studio: 581
backend/ops-тест и 23 frontend-теста; Rust: 30 тестов транспорта, помощника
и проверки решений. Эти результаты относятся к локальному прогону перед выпуском.

Локальные тесты инструментов и транспортные тесты с имитацией API сами по себе
не доказывают качество Qwen/DeepSeek. До включения необходим реальный контроль
цепочки «вызов → результат → окончательный ответ», затем предметная проверка
новой задачи и проверка пошаговой помощи студенту. Сведения о реальных проверках
фиксируются по результату, без обещания абсолютной безошибочности модели.

## Qwen continuation: измерения 2026-09-12

Узкая правка транспорта удаляет `reasoning_content` только из возвращаемого
Qwen assistant-сообщения в следующем запросе. Thinking модели не выключается;
`role`, `content`, `tool_calls` и их ID сохраняются. DeepSeek продолжает получать
свой reasoning в памяти диалога; в audit он не записывается.

Проверки использовали реальный streaming adapter, Yandex Qwen3.6-35b-a3b,
локальный authenticated hardened HTTP gateway, calculator v2 / SymPy v2.
JSON Schema и `tool_choice=auto` сохранялись. Это отдельные пробы, не строгий
парный A/B-эксперимент; единственная причина повторов ими не установлена.

| Проверка | Фактический результат | LLM-запросы | Tokens | Время |
| --- | --- | ---: | ---: | ---: |
| До правки: `ln(34.38)` | 8 одинаковых tool calls, final только после штатного `none` | 9 | 20347 | 9,76 с |
| Изоляционная проба без reasoning replay: `ln(34.38)` | один calculator result, корректный schema-final | 2 | 2079 | модельные запросы 0,90 + 0,89 с |
| После правки: `ln(12.5)` | повторный tool call вместо final; повтор не исполнялся | 2 | 2065 | 2,73 с |
| После правки: `sqrt(81)+exp(0)` | один tool call, корректный final `10` | 2 | 2008 | 2,30 с |
| После правки: `0.75/0.10` | один tool call, корректный final `7.5` | 2 | 2061 | 1,69 с |
| После правки: derivative `cA0*exp(-k1*t)` по `t` | SymPy дал корректную производную; затем модель запросила calculator с символами вместо final; второй tool не исполнялся | 2 | 2260 | 2,01 с |
| После правки: `universal_gas_constant_si` | один lookup, final без повтора, но вместо запрошенного значения модель вложила record в строку | 2 | 2429 | 2,30 с |

Для производной явно передавались symbols `cA0`, `k1`, `t`. Для reference probe
использовался исходный приватный CSV через локальный путь только для чтения;
модели передавалась одна запрошенная запись, не таблица. Исходная точность
значения сохранялась, однако соответствие конкретной инструкции final не прошло.
В диагностических пробах после второго нежелательного tool call дальнейшие
платные запросы не отправлялись. Tokens в таблице включают оба реально выполненных
LLM-запроса, в том числе последний, остановленный диагностической обёрткой.

Итог: частичный положительный эффект, **не устранение повторов и не 3/3 успешных
проверки всех инструментов**. Включение tools остаётся на hold. Новые классификаторы,
автоматическая смена модели, отключение thinking и скрытая генерация замены
не добавлены. Приватные JSON artifacts сохранены вне Git, без ключей и reasoning;
данный раздел не содержит полной справочной записи или таблицы.

## Последующая доводка контракта

Для включённых инструментов генератора и проверяющего первый запрос использует
`tool_choice=required`, дальнейшие — `auto`. У помощника и проверки студенческого
решения остаётся `auto`: подсказка первого шага не требует искусственного расчёта.
Это исполнение инструмента, не дополнительный оценщик или фильтр допуска.

Calculator v3 возвращает `unit:null, unit_inference:not_performed`: арифметика
не выводит физические размерности. Изменение версии отличает новые trace от v2.
Примеры Аррениуса согласованы с блюпринтом: студент находит E_a и k при третьей
температуре; оба результата независимо проверены локальной Decimal-арифметикой.

В реальном `run_tutor_reply` три сценария — округление ln(34.38), производная
концентрации первого порядка и lookup R — завершились одним вызовом соответствующего
инструмента и ответом (4.85 / 4.21 / 3.11 с). Будущие ответы не раскрывались.
Это три контрольных случая, не статистическая гарантия.

Проверка той же задачи с намеренно внесённой ошибкой ×10 в конечном ответе дала
полную исправленную задачу с неизменным условием: DeepSeek, пять calculator-v3
вызовов одним пакетом, два модельных запроса, 97.46 с, 21934 токена. Исправлены
и промежуточные строки. Ошибка была внесена для QA, не сгенерирована моделью.

При пустом final после успешного вычисления адаптер один раз продолжает тот же
диалог с сохранёнными данными и tool results, `tool_choice=none`. Вычисления не
повторяются, новая задача не создаётся. Повторно пустой ответ, сетевые ошибки,
отказы и пустой ответ до успешных вычислений не запускают новую генерацию.
Продолжение учитывается в usage и укладывается в общий deadline.

На реальной новой задаче Аррениуса такое завершение сработало: 32.95 с, четыре
модельных запроса, два расчёта, одно завершение, 39169 токенов. Получены правильные
E_a=74999.4127198212 Дж/моль и k320=0.16373040843134 с⁻¹ по исходным данным.
В тексте генератора оставались неточные последние цифры отдельного логарифма.
Независимый verifier получил правильный логарифм инструментом (четыре вычисления,
два запроса, 34.36 с, 17094 токена), но оставил corrected_task=null.
Поэтому обновлён контракт: в режиме tools verification.solution/answer — полный
проверенный учебный эталон и чистый ответ. При pass они сохраняются в той же задаче,
даже если отдельный corrected_task не понадобился. Исходный fingerprint проверяется
до применения; условие, rubric, ID не меняются. Tools-off поведение прежнее.

Одна предыдущая диагностическая обёртка ошибочно повторно вызвала verifier при
импорте helper; её следующая генерация завершилась пустым final, usage генерации
не сохранился. Эти данные не учитываются как успешная проверка или нулевая стоимость.
Импорт исправлен; последующие пробы сохраняли usage и не меняли БД.

Записанные здесь модельные пробы выполнены на реальных Yandex-моделях с локальным
закрытым gateway. Они не подменяют отдельную проверку опубликованной студенческой
цепочки после deployment. Абсолютная безошибочность модели не заявляется.

## Реальные role-prompts и полный candidate: 2026-09-12

После уточнения общего контракта интерпретации результатов выполнены три пробы
через реальный `run_tutor_reply`, редактируемый tutor prompt и streaming adapter.
В отличие от предыдущих микропроб, искусственного final `{answer: string}` и
JSON Schema здесь не было. Везде `tool_choice=auto`, один успешный инструмент,
затем ответ студенту; без повторов и смены модели.

| Реальный вопрос | Результат | LLM-запросы | Tokens | Время |
| --- | --- | ---: | ---: | ---: |
| «Получил ln(34.38)=3.54, верно?» в контексте Аррениуса | calculator: 3.537474999954703…; tutor подтвердил округление, не сообщил будущий численный Ea | 2 | 8038 | 4,85 с |
| Производная cA0*exp(-k1*t), явно объявленные symbols | SymPy: -cA0*k1*exp((-k1)*t); tutor подтвердил производную, не отправлял символы в calculator | 2 | 7887 | 4,21 с |
| Газовая постоянная из доступного справочника | один reference_db lookup; tutor ответил R=8.314 Дж/(моль·К), сохранил точность источника, без сериализации record | 2 | 7376 | 3,11 с |

Хеш tutor prompt: `1f975d4bb3157fb74c9b97dfbb1c1a1809de68688dd18e55231b0b704c9768ba`.
Хеш общей инструкции в этих трёх пробах:
`7c0373a8561fa0fad289320677e0603347936ed0643674c1b4c25e5f1735c9e1`.
Последующие уточнения про завершение отдельной операции, resource error и
`unit=null` добавлены после этих измерений; не приписываем их проверенному снимку.

Затем выполнен один реальный `generate_tasks` с полным физхимическим schema,
Arrhenius easy blueprint и редактируемым generator prompt. Qwen вернул один
candidate: k1=0.0150 с⁻¹, T1=300 K, Ea=75.0 кДж/моль, найти k при 323 K.
Пять модельных запросов, четыре различных calculator calls, 46021 tokens,
30,68 с. Полный schema разобран успешно; повторной генерации не было.
Независимый локальный Decimal-пересчёт даёт ln(k2/k1)=2.141190472112018…,
k2/k1=8.509561997544712…, k2=0.1276434299631707… с⁻¹: итог 0.1276 верен.
Однако в reference_solution есть неточные промежуточные значения
(0.000237364 вместо 0.000237358101…; 8.509506 вместо 8.509561998…).
Кроме того, blueprint просил найти Ea и k при промежуточной температуре либо A;
candidate вместо этого задаёт Ea и спрашивает только k. Это не полное соблюдение blueprint.

Этот же in-memory task `probe-arrhenius-same-task` передан реальному
`run_physical_validation`: DeepSeek-v4-flash, reasoning effort high,
редактируемый verifier prompt, без смены задачи и без записей в БД.
Ответ `validated`, correction=null: один запрос, 7659 tokens, 22,15 с,
**ноль tool calls**. Verifier не исправил промежуточную арифметику, назвав её
разницей округления; его собственные промежуточные числа также неточны.
Поэтому эта проба доказывает прохождение полного schema и same-task pipeline,
но НЕ подтверждает tool-use verifier или достаточное качество содержательной проверки.
Включение tools остаётся на hold до решения владельца и предметного review.

Generator prompt SHA256: `097802225ed18ce65e64721c83e9ae0ab7e548a8cb1242613c9aa12f578d58c9`;
verifier prompt SHA256: `4e8428a707356049a3c73bcc60b9137c580f9974ab938a92a3c233ee770b7687`.
Приватные artifacts `real-tutor-microtests.json` и
`real-arrhenius-generator-verifier.json` сохранены вне Git без ключей и reasoning.
В них честно сохранена фактическая дорелизная версия calculator v2 с unit=null.
Финальный gateway использует calculator v3 из-за изменения семантики единиц;
старые traces не переименовываются. Клиент не ограничивает версии whitelist.
Текущий полный прогон Main: 584 backend/ops tests passed, 42 deprecation warnings.
