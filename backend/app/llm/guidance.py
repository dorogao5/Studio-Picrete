GENERAL_BEST_PRACTICES = """
1. Начните с чёткого определения роли и экспертизы («Вы — преподаватель аналитической химии с 20-летним стажем...»).
2. Явно опишите задачу, входные данные и их особенности (OCR-расшифровка может содержать ошибки распознавания
   формул, индексов и коэффициентов — модель должна отличать ошибку студента от артефакта OCR).
3. Зафиксируйте критерии оценивания в виде нумерованного списка с максимальными баллами по каждому критерию.
4. Задайте строгий формат ответа (JSON-схема) и потребуйте отвечать только валидным JSON без пояснений вокруг.
5. Опишите поведение в граничных случаях: нечитаемое решение, пустой ответ, решение не той задачи,
   частично верное решение, верный ответ при неверном методе.
6. Потребуйте консервативности: при сомнении — снижать уверенность и помечать для проверки преподавателем,
   а не угадывать.
7. Инструкции должны быть позитивными («делайте X»), а не только запретами.
8. Для проверки вычислений потребуйте пересчитывать ключевые численные шаги самостоятельно, а не доверять студенту.
9. Держите терминологию дисциплины: используйте принятые в предмете обозначения, номенклатуру и единицы измерения.
10. Один промпт — одна задача. Не смешивайте генерацию заданий и проверку решений в одной инструкции.
""".strip()

FAMILY_GUIDANCE: dict[str, str] = {
    "deepseek": """
Целевая модель — DeepSeek V4 (deepseek-v4-pro / deepseek-v4-flash).
- System-сообщение поддерживается полноценно: всю постоянную инструкцию помещайте туда.
- По умолчанию включён thinking-режим: модель рассуждает сама, НЕ добавляйте «думай шаг за шагом» и
  цепочки рассуждений в промпт — это избыточно.
- В thinking-режиме параметры temperature/top_p игнорируются — не полагайтесь на сэмплирование,
  детерминированность обеспечивайте жёсткостью инструкции и схемы ответа.
- JSON-вывод поддерживается (response_format json_object): включите слово «json» и полную схему в промпт.
- Контекст до 1M токенов — можно передавать длинные OCR-расшифровки целиком, не сокращая.
""".strip(),
    "qwen": """
Целевая модель — Qwen 3.x (Alibaba Model Studio: qwen3.7-max / qwen3.7-plus / qwen3.6-flash).
- System-сообщение поддерживается; инструкцию давайте структурированно, с markdown-заголовками.
- Модель гибридного рассуждения: сама решает, сколько думать; не вставляйте искусственные CoT-инструкции.
- Модель мультиязычная: явно укажите «отвечайте только на русском языке», иначе возможны вставки
  на английском или китайском.
- JSON-режим включается через response_format; продублируйте схему в промпте.
- Для строгого следования формату помогает финальное напоминание в конце промпта (последним строкам
  инструкции Qwen следует охотнее всего).
""".strip(),
    "yandexgpt": """
Целевая модель — YandexGPT 5.x (Yandex AI Studio).
- System-сообщение поддерживается; русский язык — родной для модели, пишите промпт по-русски.
- Температура строго в диапазоне 0–1; для проверки решений используйте 0–0.2.
- Контекстное окно 128K, лимит ответа меньше, чем у DeepSeek/Qwen: промпт делайте компактным,
  без повторов, JSON-схему — минимально необходимой; не просите переписывать весь OCR в ответе.
- Строгого JSON-режима нет: потребуйте «только валидный JSON без дополнительного текста» и опишите
  каждое поле схемы одной строкой.
- Формулируйте задачу нейтрально-академически: у модели строгие встроенные фильтры.
""".strip(),
    "alice": """
Целевая модель — Alice AI LLM (Yandex AI Studio).
- Сильнейшая российская модель, оптимизирована под диалог и агентные сценарии; русский — родной язык.
- System-сообщение поддерживается; держите инструкцию компактной и однозначной.
- Строгого JSON-режима нет: жёстко потребуйте «в ответе только валидный JSON-объект, без текста
  до или после» и приведите схему целиком.
- Температура в диапазоне 0–1; для проверки — ближе к 0.
""".strip(),
    "gpt": """
Целевая модель — OpenAI GPT-5.5 (используется в платформе только как архитектор промптов).
- Reasoning-модель: параметр temperature НЕ поддерживается (только значение по умолчанию),
  глубина управляется reasoning_effort — не закладывайте инструкции про температуру.
- Полноценно поддерживает system-сообщение и structured outputs / json_object.
- Модель сама планирует рассуждение — формулируйте требования к результату, а не к процессу мышления.
""".strip(),
    "generic": """
Целевое семейство модели неизвестно (OpenAI-совместимый API).
- Помещайте инструкцию в system-сообщение; не полагайтесь на поддержку JSON-режима —
  жёстко опишите требуемую структуру JSON в тексте и запретите текст вне JSON.
- Не используйте специфичные для вендора возможности и параметры.
""".strip(),
}


def guidance_for(family: str) -> str:
    return FAMILY_GUIDANCE.get(family, FAMILY_GUIDANCE["generic"])
